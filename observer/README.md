# Observer (P4 — not started)

Vite + React + three.js/R3F + Zustand app reading the WebSocket ledger stream
or exported ledger JSON. Zero authority over the simulation; headless is the
primary mode. Scaffolded when P4 opens (parallel after P1's ledger stream).
