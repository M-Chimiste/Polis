{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://polis.local/schemas/memory_record.schema.json",
  "title": "MemoryRecord",
  "description": "Append-only memory-stream record. Embeddings are a storage concern (memory_records.embedding in Postgres), not part of the contract. Citations are the data for belief-divergence analysis.",
  "type": "object",
  "required": ["id", "run_id", "agent_id", "kind", "tick", "text", "importance"],
  "additionalProperties": false,
  "properties": {
    "id": {"type": "string", "format": "uuid"},
    "run_id": {"type": "string", "format": "uuid"},
    "agent_id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
    "kind": {"enum": ["observation", "plan", "reflection", "conversation_summary"]},
    "tick": {"type": "integer", "minimum": 0},
    "text": {"type": "string", "minLength": 1},
    "importance": {
      "description": "1–10, scored at write time by the fast tier",
      "type": "integer",
      "minimum": 1,
      "maximum": 10
    },
    "citations": {
      "description": "memory_record ids this record synthesizes (reflections; persisted as citation edges)",
      "type": "array",
      "items": {"type": "string", "format": "uuid"}
    }
  }
}
